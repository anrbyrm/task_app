name: task_app
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  lint: ^1.3.0
  meta: ^1.2.4
  http: ^0.12.2 
  flutter_hooks: ^0.15.0
  cupertino_icons: ^1.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.11.1


flutter:
  uses-material-design: true